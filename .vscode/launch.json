/* https://github.com/microsoft/vscode-cpptools/issues/4919 */
{
    "configurations": [
        {
            "name": "Debug with Meson",
            "type": "cppdbg",
            "request": "launch",
            "target": "./builddir/main",
            "program": "${fileDirname}/builddir/${fileBasenameNoExtension}",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build debug meson"
        }
    ]
}