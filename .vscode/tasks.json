{
    "tasks": [
        {
            "label": "build debug meson",
            "type": "shell",
            "command": "meson builddir --buildtype=debug && cd builddir && ninja"
        }
    ],
    "version": "2.0.0"
}